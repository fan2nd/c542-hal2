# Letter Shell

Source: https://github.com/NevermindZZT/letter-shell

Pinned commit: `03432e824ca889d961743b437bce3f837c47bb86`.

Files copied without modification: `shell.c`, `shell.h`, `shell_ext.c`,
`shell_ext.h`, `shell_cfg.h`, `LICENSE` (MIT).

The project provides `../shell_config.h` through `SHELL_CFG_USER`. Its command
table lives in `../app_console.c`; the upstream example command table and optional
extensions are not compiled.
